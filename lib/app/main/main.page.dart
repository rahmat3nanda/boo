import 'package:flutter/material.dart'
    show Placeholder, StatelessWidget, Widget;

class MainPage extends StatelessWidget {
  const MainPage({super.key});

  @override
  Widget build(_) => const Placeholder();
}
